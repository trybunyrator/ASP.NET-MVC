@model IEnumerable<Prototyp2.Views.grob>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Dodaj nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Koordynaty")
            @*
        @Html.DisplayNameFor(model => model.koordynaty)
            *@
        </th>
        <th>
            @Html.DisplayName("Typ grobu")
            @*
        @Html.DisplayNameFor(model => model.groby_rodzaj.nazwa)
            *@
        </th>
        <th>
            @Html.DisplayName("Nazwa miejscowości")
            @*
        @Html.DisplayNameFor(model => model.miejscowosc.nazwa )
            *@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.koordynaty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.groby_rodzaj.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.miejscowosc.nazwa)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.id_grob }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.id_grob }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.id_grob })
        </td>
    </tr>
}

</table>
